# ── Unreal Engine temporary folders ──
QuietRiftEnigma/Intermediate/
QuietRiftEnigma/Saved/
QuietRiftEnigma/DerivedDataCache/
QuietRiftEnigma/Binaries/
QuietRiftEnigma/Build/

# ── IDE / build tooling ──
.vs/
*.sln
*.vcxproj*
*.suo
*.xcworkspace/

# ── Python / Blender script cache ──
__pycache__/
*.pyc
*.pyo

# ── Free Fab content packs (60+ GB local, never push) ──
# These live on disk for development reference but stay out of git.
# Re-track individual subdirs explicitly if you need a specific asset
# from a pack to ship with the project.
QuietRiftEnigma/Content/Fabs/

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini
